import { CalculatetotalpricePipe } from './calculatetotalprice.pipe';

describe('CalculatetotalpricePipe', () => {
  it('create an instance', () => {
    const pipe = new CalculatetotalpricePipe();
    expect(pipe).toBeTruthy();
  });
});
