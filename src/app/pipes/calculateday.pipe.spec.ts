import { CalculatedayPipe } from './calculateday.pipe';

describe('CalculatedayPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculatedayPipe();
    expect(pipe).toBeTruthy();
  });
});
